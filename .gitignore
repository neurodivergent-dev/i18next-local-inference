node_modules/

# LocaleSmith desktop app (commercial, not part of the open-source repo)
desktop/

# i18n Dashboard state (cache + AI approvals) — kept under .i18n-dash/ in the target repo
.i18n-dash/

# Legacy state location of older versions (still read for backward compatibility)
scripts/.i18n-confirmed-same.json
scripts/.i18n-translation-cache.json
